#    -*- mode: org -*-

This folder and sub-folders contain files used during testing.

* txe
Contains compiled t files (.txe) used for testing the wrapper interface
to access the contents of compiled t (.txe) files.
These files are copies of files located within the canlib repository
used for testing the CANlib kvScriptTxeGetData function.

